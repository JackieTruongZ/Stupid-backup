// window.addEventListener('scroll', function () {
//   const imageContainer = document.getElementById('page');
//   const scrollY = window.pageYOffset || document.documentElement.scrollTop;
//   //imageContainer.style.top = `50%`;
//   imageContainer.style.left = `${-100 + scrollY * 0.5}%`;

//   const imageContainer1 = document.getElementById('page2');
//   const scrollY1 = window.pageYOffset || document.documentElement.scrollTop;
//   //imageContainer1.style.top = `50%`;
//   imageContainer1.style.left = `${-100 + scrollY1 * 0.5}%`;

// });   



